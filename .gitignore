# Code-signing private keys must never be committed.
*.pfx
*.p12
*.pvk
*.key
signing-secrets/

# Generated outputs and local Gradle state are never part of the public source release.
/build/
/.gradle/

# Local editor and agent configuration.
/.idea/
/.codex/
